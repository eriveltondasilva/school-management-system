import { InputError, InputText, InputTextarea } from './Input'

// ---------------------------------------------------------------------------
const Input = {
  Text: InputText,
  Textarea: InputTextarea,
  Error: InputError,
}

export default Input
